In order to run the file browser execute the following command in the same directory with the build.xml fil:

ant run

In order to delete the build directory and the corresponding .class/.jar files execute in the same directory:

ant clean
